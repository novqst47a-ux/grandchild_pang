# OS / 에디터 잡파일
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# 로컬 패키지 캐시(현재 PWA에는 npm 의존성이 없지만 실수로 생성될 수 있음)
/webapp/node_modules/
/webapp/.npm-cache/

# 디자인 원본과 개인 도구 설정
/img_original/
/.claude/settings.local.json

# 기획·디자인 문서. 저장소에서 내리고 각자 기기에만 둔다.
# 지난 내용은 지워지지 않고 이 커밋 이전 이력에 그대로 남아 있다.
/docs/
