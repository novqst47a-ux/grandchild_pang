# Godot 에디터 바이너리 (내려받은 실행 파일, 저장소에 넣지 않음)
/Godot_v4.7.1-stable_win64/

# Godot 연습 프로젝트 (웹앱 전환 검토용 로컬 참고물)
/practice/

# OS / 에디터 잡파일
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# 웹앱은 번들러를 쓰지 않으므로 빌드 산출물이 없다. webapp/ 자체가 배포본이다.
# 의존성도 없지만, 실수로 npm install을 돌렸을 때를 대비해 남겨 둔다.
/webapp/node_modules/

# 축하 배지 원본(PSD). 결과 PNG만 저장소에 둔다.
/img_original/

# Claude Code 로컬 설정 (실행 설정 launch.json은 공유한다)
/.claude/settings.local.json
