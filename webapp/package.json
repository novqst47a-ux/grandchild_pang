{
  "name": "sonjupang-webapp",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "test": "node --test",
    "check": "node --check src/game-core.js && node --check src/custom-blocks.js && node --check src/storage.js && node --check src/app.js && node --check scripts/build.mjs && node --check scripts/verify-build.mjs && node --check scripts/build-android.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "build": "vite build && node scripts/build.mjs && node scripts/verify-build.mjs",
    "android:sync": "npm run build && cap sync android",
    "android:open": "cap open android",
    "android:apk": "npm run android:sync && node scripts/build-android.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "vite": "^8.2.0"
  }
}
